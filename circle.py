class DiscretizationCircles(): 

    @staticmethod 
    def basicAlgorithm(start_point, radius):
        """
        start_point--> tuple(x,y)
        finish_point--> tuple(x,y)
        Return a list of tuples, every tuple is like (x,y) point, where the line will be printed 
        """
        coor_x=start_point[0]
        coor_y=radius

        decision_parameter=1-radius

        while coor_x==start_point[0]  and : 


